# HotelLux - Complete Setup Guide

## Files Created

✅ **`.env.example`** - Template for environment variables
✅ **`generate-secrets.js`** - Script to generate secure JWT and session secrets
✅ **`database_export_complete.sql`** - Complete database with all tables and data
✅ **`POSTGRESQL_LOCAL_SETUP.md`** - Local PostgreSQL setup guide

## Quick Start

### 1. Generate Secrets
```bash
node generate-secrets.js
```

### 2. Setup Environment Variables

#### For Replit:
Go to **Secrets** tab and add:
- `JWT_SECRET` (từ generate-secrets.js)
- `SESSION_SECRET` (từ generate-secrets.js)  
- `GOOGLE_CLIENT_ID` (từ Google Cloud Console)
- `GOOGLE_CLIENT_SECRET` (từ Google Cloud Console)

#### For Local Development:
```bash
cp .env.example .env
# Edit .env with your values
```

### 3. Google OAuth Setup

Current callback URLs cần thêm vào Google Cloud Console:
- **Replit**: `https://15bbd97e-5fe7-46f4-af19-2831f63b4964-00-2mdengl1ik20m.spock.replit.dev/api/auth/google/callback`
- **Local**: `http://localhost:5000/api/auth/google/callback`

### 4. Database Setup

#### For Replit:
Database tự động có sẵn, chỉ cần:
```bash
npm run db:push
```

#### For Local PostgreSQL:
```bash
# Run setup script
./setup-postgresql.sh  # Linux/Mac
setup-postgresql.bat   # Windows

# Import database
psql -U postgres -d hotellux -f database_export_complete.sql
```

## Features Available

✅ **Authentication**: JWT + Google OAuth
✅ **Database**: PostgreSQL with 16 tables
✅ **Payment**: Stripe integration
✅ **Multi-language**: Vietnamese/English
✅ **Real-time**: WebSocket chat
✅ **Reviews**: 5-star rating system  
✅ **Loyalty**: Points program
✅ **Booking**: Deposit-based payments
✅ **Admin**: Complete dashboard
✅ **Email**: SendGrid integration

## Test Accounts

### Admin
- Email: `admin@hotellux.com`
- Password: `admin123`

### Customer  
- Email: `customer@hotellux.com`
- Password: `customer123`

## API Keys Needed

### Required:
- `JWT_SECRET` - Generated by script
- `SESSION_SECRET` - Generated by script
- `GOOGLE_CLIENT_ID` - From Google Cloud Console
- `GOOGLE_CLIENT_SECRET` - From Google Cloud Console

### Optional:
- `STRIPE_SECRET_KEY` - For payments
- `VITE_STRIPE_PUBLIC_KEY` - For payments  
- `SENDGRID_API_KEY` - For email notifications

## Support Files

📋 **POSTGRESQL_LOCAL_SETUP.md** - Local PostgreSQL setup guide

## Troubleshooting

### Google OAuth Error 400
- Check callback URL trong Google Cloud Console
- Verify GOOGLE_CLIENT_ID và GOOGLE_CLIENT_SECRET
- URL phải match exactly với config

### Database Connection Error
- Verify DATABASE_URL
- Check PostgreSQL service running
- Run `npm run db:push` để sync schema

### Payment Issues
- Test với Stripe test keys
- Verify STRIPE_SECRET_KEY format
- Check webhook endpoints nếu cần

## Next Steps

1. ✅ Add secrets to environment
2. ✅ Configure Google OAuth
3. ✅ Test login functionality
4. ✅ Test booking system
5. ✅ Configure payment processing
6. ✅ Deploy to production

Tất cả files cần thiết đã được tạo! Hệ thống sẵn sàng cho cả development và production.